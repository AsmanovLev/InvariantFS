# old_docs — archived documentation

Historical design/audit papers kept for reference. They describe the
**v2 (pre-redesign) system** and earlier. Nothing here is normative: the
current design lives in `../design-meta-v3.md`, and the code is the
de-facto spec.

Moved 2026-09-19 as part of the metadata-v3 cleanup.

## Contents

- `AUDIT.md` — original doc↔source consistency audit (v2 era).
- `REPO-CLEANUP.md` — `var/` git-history purge notes (prepared, not run).
- `WP*.md` — per-work-package design rationale, WP1–WP69.

Superseded by `design-meta-v3`: WP30/WP35 (mapper), WP41 (stats), WP42
(sweep-mapper), WP43 (heat), WP44 (dedupe), WP52 (owner-append), WP53
(ckp-registry), WP58/58a/58b (meta-record-v3), WP65 (rename-checkpoint).

Active docs remain in `impl_docs/` (`design-meta-v3.md`, `DOCMAP.md`,
`FILEMAP.md`, `FUNCTIONS.md`, `TYPES.md`, `INVFS-ARCH.md`,
`WP-TEMPLATE.md`).
